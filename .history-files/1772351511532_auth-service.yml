server:
  port: 8083

spring:
  application:
    name: auth-service

  # data:
  mongodb:
    uri: ${MONGO_AUTH_URI}

security:
  jwt:
    secret: ${JWT_SECRET}
    access-token-expiration: 900000        # 15 min
    refresh-token-expiration: 604800000    # 7 days
    issuer: 

auth:
  refresh:
    rotation-enabled: true